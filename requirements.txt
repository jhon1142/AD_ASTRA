# ══════════════════════════════════════════════════════════════════════════════
# AD_ASTRA — Dependencias
# ══════════════════════════════════════════════════════════════════════════════

# ── Entorno ───────────────────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── Loaders ───────────────────────────────────────────────────────────────────
pypdf==4.3.1                        # PDFLoader
beautifulsoup4==4.12.3              # HTMLLoader, RemoteLoader
lxml==5.2.2                         # Parser HTML
requests==2.32.3                    # RemoteLoader
openpyxl==3.1.5                     # XLSXLoader
pytesseract==0.3.13                 # ImageLoader (OCR)
Pillow==10.4.0                      # ImageLoader

# ── Preprocessing ─────────────────────────────────────────────────────────────
langdetect==1.0.9                   # language.py

# ── Chunking / Tokenización ───────────────────────────────────────────────────
tiktoken==0.7.0                     # token_counter.py, tokenizer.py
spacy==3.7.5                        # sentence_splitter.py (opcional)

# ── Embeddings ────────────────────────────────────────────────────────────────
openai==1.35.3                      # Encoder (OpenAI)
sentence-transformers==3.0.1        # Encoder (SentenceTransformers)
cohere==5.5.8                       # Encoder (Cohere)

# ── Vectorstore ───────────────────────────────────────────────────────────────
faiss-cpu==1.8.0                    # FAISSManager

# ── Utilidades ────────────────────────────────────────────────────────────────
numpy==1.26.4
tqdm==4.66.4
pydantic==2.7.4

# ── LLM / Agente (app/) ───────────────────────────────────────────────────────
langchain-core==0.2.10
langgraph==0.1.19

# ── Datos geoespaciales (opcional, solo si usas PBFLoader) ────────────────────
# osmium==3.7.0
